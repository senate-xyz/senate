{
  "name": "@senate/detective",
  "version": "1.0.0",
  "description": "This service takes care of sending notifications",
  "author": "Senate",
  "private": true,
  "license": "GNU GPLv3",
  "scripts": {
    "build": "cargo b",
    "build:detective": "cargo b",
    "start": "cargo r",
    "start:detective": "cargo r",
    "clean": "cargo clean",
    "lint": "cargo fmt",
    "lint:fix": "cargo clippy --fix --allow-dirty",
    "prettier": "cargo fmt",
    "prettier:fix": "cargo fmt",
    "db:generate": "cargo generate",
    "update": "cargo update && cargo upgrade"
  }
}
